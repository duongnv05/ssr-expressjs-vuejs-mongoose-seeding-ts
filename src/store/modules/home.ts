export default {
    state: () => ({
        item: [{
            ok: 0,
            message: "init"
        }]
    }),
    mutations: {
        // 'SET_ITEM'(state: any, {item}) {
        //     // const items = [...state.item]
        //     // items.push(item)

        //     state.item = item
        // }
    }
}